Unordered Multiset

allows duplicate valuesvalues are not ordered
value will be identified by itself
value cannot be modified


memebr function of unordered_multiset
1. Insert 
single element O(1) avg    || O(N) worst

multiple element  O(n) avg || O(n*(N+1)) worst


2. deletion
   erase()    O(1) avg  || O(N) worst
3. find() O(1) avg  || O(N) worst

4. count():
    O(n) avg  
    || O(N) worst

 